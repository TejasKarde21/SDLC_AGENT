"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareRunErrorFinalOutput = exports.resolveRunErrorHandler = exports.validateRunErrorHandlerFinalOutput = exports.createRunErrorFinalOutputItem = exports.formatRunErrorFinalOutput = exports.attachRunStateToError = void 0;
const errors_1 = require("../errors.js");
const message_1 = require("../helpers/message.js");
const items_1 = require("../items.js");
const items_2 = require("./items.js");
const streaming_1 = require("./streaming.js");
/**
 * Attaches the active run state to nested tool guardrail tripwire errors without replacing them.
 */
const attachRunStateToError = (error, state) => {
    if (!(error instanceof errors_1.ToolCallError)) {
        return;
    }
    if (error.error instanceof errors_1.ToolInputGuardrailTripwireTriggered ||
        error.error instanceof errors_1.ToolOutputGuardrailTripwireTriggered) {
        error.error.state ??= state;
    }
};
exports.attachRunStateToError = attachRunStateToError;
const buildRunData = (state) => ({
    input: state._originalInput,
    newItems: state._generatedItems,
    history: (0, items_2.getTurnInput)(state._originalInput, state._generatedItems, state._reasoningItemIdPolicy),
    output: (0, items_2.getTurnInput)([], state._generatedItems, state._reasoningItemIdPolicy),
    rawResponses: state._modelResponses,
    lastAgent: state._currentAgent,
    state,
});
const formatFinalOutput = (agent, finalOutput) => {
    if (agent.outputType === 'text') {
        return String(finalOutput);
    }
    return JSON.stringify(finalOutput);
};
const createFinalOutputItem = (agent, outputText) => new items_1.RunMessageOutputItem((0, message_1.assistant)(outputText), agent);
function validateRunErrorFinalOutput(agent, outputText) {
    try {
        agent.processFinalOutput(outputText);
    }
    catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        throw new errors_1.UserError(`Invalid run error handler finalOutput: ${message}`);
    }
}
exports.formatRunErrorFinalOutput = formatFinalOutput;
exports.createRunErrorFinalOutputItem = createFinalOutputItem;
exports.validateRunErrorHandlerFinalOutput = validateRunErrorFinalOutput;
const resolveRunErrorHandler = async ({ error, errorKind, errorHandlers, context, runData, }) => {
    const kind = errorKind ??
        (error instanceof errors_1.MaxTurnsExceededError
            ? 'maxTurns'
            : error instanceof errors_1.ModelRefusalError
                ? 'modelRefusal'
                : undefined);
    if (!kind) {
        return undefined;
    }
    const typedError = kind === 'maxTurns' && error instanceof errors_1.MaxTurnsExceededError
        ? error
        : kind === 'modelRefusal' && error instanceof errors_1.ModelRefusalError
            ? error
            : kind === 'invalidFinalOutput' && error instanceof errors_1.ModelBehaviorError
                ? error
                : undefined;
    if (!typedError) {
        return undefined;
    }
    const handler = errorHandlers?.[kind] ?? errorHandlers?.default;
    if (!handler) {
        return undefined;
    }
    const handlerResult = await handler({
        error: typedError,
        context,
        runData,
    });
    return handlerResult || undefined;
};
exports.resolveRunErrorHandler = resolveRunErrorHandler;
const prepareRunErrorFinalOutput = async ({ error, state, errorHandlers, streamResult, }) => {
    const handlerResult = await (0, exports.resolveRunErrorHandler)({
        error,
        errorHandlers,
        context: state._context,
        runData: buildRunData(state),
    });
    if (!handlerResult) {
        return false;
    }
    const includeInHistory = handlerResult.includeInHistory !== false;
    const outputText = formatFinalOutput(state._currentAgent, handlerResult.finalOutput);
    validateRunErrorFinalOutput(state._currentAgent, outputText);
    streamResult?._hideFinalOutput();
    state._lastTurnResponse = undefined;
    state._lastProcessedResponse = undefined;
    const item = createFinalOutputItem(state._currentAgent, outputText);
    if (includeInHistory) {
        state._generatedItems.push(item);
    }
    if (streamResult) {
        (0, streaming_1.streamStepItemsToRunResult)(streamResult, [item]);
    }
    state._currentStep = {
        type: 'next_step_final_output',
        output: outputText,
    };
    state._finalOutputSource = 'error_handler';
    return true;
};
exports.prepareRunErrorFinalOutput = prepareRunErrorFinalOutput;
//# sourceMappingURL=errorHandlers.js.map